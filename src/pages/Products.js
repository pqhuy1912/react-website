import Layout from '../components/Layout';

function Products() {
    return (
        <Layout>
            <h1 className="products title">PRODUCTS</h1>
        </Layout>
    );
}

export default Products;
